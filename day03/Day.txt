if 语句：一个if是一个整体。
if 语句的嵌套：
　if 语句本身是由多条子句组成的一条复合语句。
　if 语句本身可以作为语句嵌套到另一个
条件表达式：
　语法：
　　表达式１　ｉｆ真值表达式　ｅｌｓｅ表达式２
　作用：
　　根据真值表达式的取值（ｔｒｕｅ／ｆａｌｓｅ）来决定执行表达式

pass 语句
　作用：同创用来填充语法空白
　　　pass有名空语句
　语法：
　　　pass

布尔运算（交互界面可以用）：
　运算符：not(非)　　and(与)　　or(或)
布尔非操作　not
　语法
　　not x
　作用：对ｘ进行布尔取非，如ｂｏｏｌ（ｘ）为ｔｕｒｅ，则返回ｆａｌｓｅ
布尔与操作　ａｎｄ
　语法
　　ｘ　ａｎｄ　ｙ
　　注：ｘ，ｙ代表表达式。
　作用：有限返回假值对象，当ｘ的布尔值为ｆａｌｓｅ时，返回ｘ否则返回ｙ。（同时为ture时，返回真。有false的，就返回假。）
布尔或运算　ｏｒ
　语法
　　ｘ　ｏｒ　ｙ
　作用：优先返回真值对象，当ｘ为ｔｒｕｅ时返回ｘ，否则返回ｙ。（都为ｆａｌｓｅ是返回错。有ｔｒｕｅ时，就返回ｔｒｕｅ。）

正负号运算符
　＋（正号）　　－（负号）
　注：一元运算符（一个元素参加运算）
　语法：
　　＋　表达式
　　－　表达式
字符串　ｓｔｒ
　作用：
　　用来记录文字（文本）信息
　表示方法：
　　在非注释中凡是用引号括起来的部分都是字符串
    '　　　单引号
    "　　　双引号
    '''　　三单引
    """　　三双引
　　空字符串的字面值表达方法
　　　''   ""   ''''''   """"""
　　非空字符串的字面值的表达方法
　　　'hello'   "hello"   '''hello'''   """hello"""
　单引号与双引号的区别
　　单引号内的双引号不算结束符
　　双引号内的单引号不算结束符

三引号字符串的作用：
　三引号字符串可以包含单引号和双引号
　三引号字符串中的换行会自动转换为换行符“/n”
隐式字符串字面值拼接


用转义序列代表特殊字符
　字符串字面值中用字符反斜杠\后跟一些字符代表特殊的一个字符
  如：
  \'　代表一个单引号
  \"　代表一个双引号
  \\　代表一个反斜杠
  \n　换行
  \r　返回光标至行首
  \f　换页
  \t　水平制表符（Ｔａｂ）
  \v　垂直制表符
  \b　倒退(退格键，一个\b为一个退格键)
  \0　空字符（字符值为０）
  \0oo oo为两位八进制表示的字符
  \xXX XX为两位十六进制表示的字符(ASCII值)
  \uXXXX  四位十六进制表示的Unicode 16字符
  \UXXXXXXXX  八位十六进制表示的Unicode 32字符

ASCII编码表
　$　man ascii<回车>
常用的ascii编码
字符   十进制　　十六进制
'0'    48       0x30
'A'    65       0x41
'a'    97       0x61

序列的概念
　字符串是序列（有序排列）
　所有的序列都能用len(x)函数求其长度

len(x)函数
　返回字符串中字符的个数

raw字符串（原始字符串）
　格式：
　　ｒ'字符串内容'
　　ｒ"字符串内容"
　　ｒ'''字符串内容'''
　　ｒ"""字符串内容"""
　作用：
　　让转义符号反斜杠　\　无效

字符串的运算：
　运算符：＋ +＝ * *=
　＋　加号运算符用于拼接字符串
　＋=　用于元字符串和右侧字符串拼接，再用变量绑定新的字符串
　* 运算符生成重复的字符串
　
字符串的比较运算
　运算符：
　　< <= > >= == !=
　比较规则：
　　依次按编码值进行两两字符比较，一旦不同，则比较结束并返回比较结果，当编码值和长度完全一样时，两个字符串相同。

In, not in运算符
　作用：
　　in用于序列，字典，集合中，用于判断某个值是否存在于容器中，如果存在则返回true，否则返回ｆａｌｓｅ
　　ｎｏｔ　ｉｎ与　ｉｎ运算符的返回值结果相反
　格式：
　　对象ｉｎ序列
　示例：
   x='i am a studing'　　 (绑定字符)
   'to' in x  　　　　　　　(判断‘ｔｏ’是否在‘ｘ’中)
   返回值为'Ture'　　　　　　(返回值)


练习：
　１、北京出租车计价器：
　　收费标准：
　　　３公里以内收费１３元
　　　基本单价２．３元／公里（超出３公里意外）
　　　空驶费：超出１５公里后，每公里加收单价的５０％（空驶费：３．４５元／公里）
　　要求：输入公里数，打印出费用金额
　２、输入一个学生的三科成绩：
　　　１）打印出最高分是多少分？
　　　２）打印出最低分是多少分？
　　　３）打印出平均分是多少分？
　３、计算ＢＭＩ指数，又称身体质量指数
　　　计算公式：
　　　　ＢＭＩ＝体重（公斤）／身高的平方（米）
　　　标准表：　
　　　　ＢＭＩ<１８．５　　体重过轻
　　　　１８．５<=BMI<=24  体重正常
　　　　ＢＭＩ>=24
　　　要求：输入身高和体重，打印出ＢＭＩ的值，并打印体重情况。
　４、打印矩形
